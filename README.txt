This file will help you run the application:

--------------------------------------------------------------------------------|
This is how to set up a virtual environment for python:

In the directory you're working in, initialize/create a virtural env:
    sudo su
    python3 -m venv .venv

Get into the env:
    . .venv/bin/activate

Now your pip install or other pip commands will work...
--------------------------------------------------------------------------------|

Ideas for the Drone tracking application:

- Have a home page with a template html all to itself
- Have a basic template page to show a drone and then other drones will be inherited from that
- Somehow make use of the intake JSON function... take in the files and put out information